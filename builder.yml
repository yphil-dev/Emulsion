# electron-builder.yml

appId: com.yPhil.Emulsion
productName: Emulsion
artifactName: "emulsion_${arch}.${ext}"
asar: true
compression: store

# Unpack the native binaries from the ASAR into this executable
asarUnpack:
  - "bin/**"

files:
  - "**/*"
  - "!**/*.{map,log,md,markdown,txt}"
  - "!**/node_modules/**/*.ts"
  - "!**/**/{test,tests,tmp,example,examples}"
  - "!**/node_modules/**/{CHANGELOG.md,README.md,LICENSE,Makefile}"

# Copy the 'bin' folder verbatim alongside the app
extraResources:
  - from: "bin"
    to: "bin"
    filter:
      - "**/*"

publish:
  provider: github
  publishAutoUpdate: false

linux:
  executableName: emulsion
  target:
    - AppImage
    - deb
    - rpm
    - dir
  icon: "img/icon.png"
  category: Utility

win:
  target:
    - nsis
  icon: "img/icon.png"

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
